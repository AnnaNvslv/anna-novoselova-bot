# План правок CRM — Гинтер Оптика

Порядок: от критичного к косметическому.

---

## 🔴 Блок 1 — Терминология и нейминг

### #1 — Переработать нейминг во всей платформе
- **Профиль пациента** — попап с данными, приёмами, заказами
- **Карта пациента** = Карта обследования (optometrijska karta)
- **Приём** = Pregled
- **Заказ** — тип: очки / КЛ / ремонт

Статус: ⚪️ частично

---

## 🔴 Блок 2 — Критический функционал

### #2 — Кнопки действий в карте обследования
- Печать, Сохранить, Пациенту (mailto + PDF), В оптику (mailto), Закрыть с подтверждением

Статус: ⚪️ не начато

### #3 — Причины обращения: синхронизация анкеты и карты
Статус: ✅ готово (Анамнез в карте обследования подтягивает данные анкеты; карточка пациента показывает полную анкету — см. запись 2026-07-10)

### #4 — Номер приёма `OG-ММГГ-НН`
Статус: ✅ готово

---

## 🟡 Блок 3 — Данные и структура

### #5 — Поле «№ заказа» (с пакета)
Статус: ✅ готово

### #11 — ID пациента `ГГНН`
Статус: ✅ готово

### #12 — Дата рождения в таблице Пациентов
Статус: ✅ уже было

### #10 — Новый пациент в заказе — перевод на сербский
Статус: ⚪️ не начато

---

## 🟡 Блок 4 — UX и рецепты

### #7 — PD: убрать предустановленное значение
Статус: ✅ уже было

### #8 — Новый заказ: выпадающие списки рецепта
Статус: ⚪️ не начато

### #9 — Новый заказ: отображение рецепта в строке (OD/OS)
Статус: ⚪️ не начато

---

## 🟡 Блок 5 — Шаблоны

### #6 — Предустановленный текст адаптации
Статус: ✅ уже было

---

## 🟢 Блок 6 — Визуализация

### #13 — Цветовые плашки пациентов

| Условие | Цвет | Текст |
|---------|------|-------|
| Заказ «готов» | Зелёная | Готov / Готов |
| Подходит контрольный визит | Красная | Kontrola / Контроль |
| Есть запланированный приём | Синяя | дата приёма |
| Заказ «в работе» | Жёлтая | u izradi / В работе |
| Заказ «оформлен» | Серая | kreirana / Оформлен |

Статус: ✅ готово

---

## 🔵 Блок 7 — Мобильная версия и вёрстка

### #14 — Нижняя навигация: z-index и перекрытие попапов
- overlay z-index выше навбара, модалки не уходят под навбар
- safe-area-inset для телефонов с вырезом/жестами
- Показ/скрытие mobile-nav через CSS media query (убрана JS-логика)
- Скрытие кнопок ervin в mobile-nav

Статус: ✅ готово (2026-06-07)

### #15 — Общий аудит вёрстки
Статус: ⚪️ не начато

---

## 📋 Журнал изменений

### 2026-07-24 (2) — Интеграция личного сайта в booking-флоу
Цель: сделать сайт (annanvslv.github.io) более заметной и обжитой частью воронки, а не второстепенной ссылкой.

- **booking.html / css/booking.css** — кнопка «Сайт» в шапке booking-страницы выделена заливкой (`hdr-pill-site`, акцентный синий фон вместо белого outline) — теперь визуально приоритетнее «Написать Анне» и «Карта».
- **js/booking.js** — ссылка «О том, как проходит приём →» в карточках видов приёма «Первичный приём» и «Подбор КЛ» переведена с поста в Telegram-канале на страницу сайта `priem.html#kak-proxodit` (константа `TG_POST` → `PRIEM_LINK`).
- **booking.html / js/booking.js** — на странице подтверждения записи (после записи, до подтверждения через бота) добавлена ссылка-плашка «Как подготовиться к приёму →» на `priem.html#podgotovka` (новый `.prep-link` в CSS, ключ перевода `prepLinkText` в ru/sr).
- **supabase/functions/telegram-bot/index.ts** — в сообщение-подтверждение пациенту после записи добавлен абзац с приглашением на сайт («Пока ждёте приём — загляните на сайт...», ссылка на `annanvslv.github.io`).
- `booking.html` → `css/booking.css?v=5`, `js/booking.js?v=6`.
- ⚠️ **Требуется ручной редеплой Edge Function** (Анна, из терминала): `npx supabase functions deploy telegram-bot --project-ref ncfqiznpilikwmpqhapb --no-verify-jwt` — правка сообщения бота не подействует, пока функция не задеплоена.

### 2026-07-24 — Фикс: "Ошибка записи" на booking.html при свободном слоте
После вчерашнего RLS-хардненинга (2026-07-23) онлайн-запись стала падать с алертом `errBook` ("Ошибка записи. Напишите нам: @AnnaNvslv") даже когда слот точно был свободен.

- **Причина**: у анонимной роли на `patients` и `appointments` теперь только `INSERT`-политика, без `SELECT`. Код `submitBooking()` делал `.insert({...}).select().single()` — PostgREST после insert пытается вернуть вставленную строку через `SELECT`, на которую у анонима нет прав; в ответ приходило 0 строк, и `.single()` бросал ошибку. Реальная вставка пациента при этом либо не происходила, либо (для appointments) происходила, но без возврата данных — в обоих случаях пользователь видел `errBook`, при этом сам слот оставался свободен (сбой был раньше апдейта `available_slots`).
- **Фикс (`js/booking.js`)**: id для `patients` и `appointments` теперь генерируются на клиенте (`crypto.randomUUID()`), `.select().single()` убран из обоих insert-ов — читать вставленную строку обратно больше не требуется, достаточно проверить `error`. Это чинит запись без расширения анонимного доступа на чтение (RLS-политики не трогали).
- `booking.html` → `js/booking.js?v=5`.

Этот фикс раскрыл ещё три независимых бага, тоже последствия вчерашней security-миграции (RLS/Auth) — тесты онлайн-записи их не задевали раньше, а с реальным анонимным трафиком все три всплыли по очереди:

- **RPC без прав**: `get_next_appointment_number(date_str text)` осталась без `GRANT EXECUTE` для роли `anon` после миграции 2026-07-23 (RLS настраивали, а грант на саму функцию не выдали). Выдано: `GRANT EXECUTE ON FUNCTION get_next_appointment_number(date_str text) TO anon;`
- **Гонка в генераторе `patient_code`**: `generate_patient_code()` считал следующий код через `SELECT MAX(...)+1 FROM patients` — неатомарно, при почти одновременных вставках два инсерта могли посчитать один и тот же номер и упасть на `patients_patient_code_key`. Переделано на атомарный счётчик: новая таблица `patient_code_counters` (`yy` PK, `seq`, RLS включён без policy — доступна только на уровне БД), инициализирована текущими максимумами по годам; функция переписана на `INSERT ... ON CONFLICT (yy) DO UPDATE SET seq = seq + 1 RETURNING seq` с `SECURITY DEFINER` (чтобы RLS на `patient_code_counters` не мешал триггеру, выполняющемуся от анонимной роли при онлайн-записи).
- **Опечатка при правке функции**: в первой версии переписанной `generate_patient_code()` локальная переменная называлась `yy` — так же, как колонка `yy` в `patient_code_counters`, Postgres не мог разрешить неоднозначность (`column reference "yy" is ambiguous`). Переменную переименовали в `v_yy`.

Проверено на живом сайте — онлайн-запись проходит.

**Подключён Supabase MCP-коннектор** (проект `ncfqiznpilikwmpqhapb`) для будущих сессий — в перспективе Claude сможет сам выполнять SQL-диагностику и миграции вместо пошагового копирования в SQL Editor.

### 2026-07-23 — Security: закрытие доступа к данным пациентов
Полный аудит и закрытие уязвимостей, из-за которых любой человек с anon key (открыт в коде на GitHub) мог напрямую читать/менять данные в обход сайта и CRM.

- **Telegram-бот**: токен отозван через BotFather и заменён (был риск попадания в переписку/логи), обновлён в `settings` и в Supabase Secrets (`BOT_TOKEN`), вебхук переустановлен. Обе Edge Function (`telegram-bot`, `send-reminders`) передеплоены.
- **js/auth.js, crm-v3.html** — вход в CRM переведён с PIN-кода (проверялся на клиенте, PIN-коды читались из `settings` анонимным ключом — были полностью доступны кому угодно) на Supabase Auth (email + пароль). PIN-логика и `localStorage.crm_role` удалены.
- **Новая таблица `user_roles`** (`user_id`, `role`, `display_name`) — связывает Supabase Auth логины с ролями CRM, RLS: пользователь видит только свою строку.
- **js/booking.js** — убран прямой клиентский доступ к `bot_token`/`my_chat_id` (раньше уходили в браузер каждого пациента при онлайн-записи и были видны в Network). Уведомление Анне о новой записи теперь отправляет новая Edge Function **`supabase/functions/notify-new-booking`** — токен читается только на сервере, service_role ключом.
- **`get_next_appointment_number(date_str)`** — новая SQL-функция (`security definer`) для генерации номера записи (`OG-ММГГ-НН`), вызывается через RPC вместо прямого `SELECT` анонимом по таблице `appointments` (раньше требовался широкий анонимный доступ на чтение всей таблицы ради подсчёта).
- **RLS включён и настроен на всех таблицах проекта** (`ncfqiznpilikwmpqhapb`): `patients`, `appointments`, `available_slots`, `examinations`, `orders`, `settings`, `booking_log`, `budget_*`. До этого RLS был выключен почти везде (`rowsecurity=false`) — данные были открыты полностью.
  - `patients`, `appointments` — анонимам только `insert` (нужно для онлайн-записи), полный доступ — только авторизованным.
  - `available_slots` — анонимам разрешён `select` (публичное расписание) и `update` только в направлении «свободен → забронирован» (нельзя освободить чужую бронь).
  - `examinations`, `orders`, `settings`, `budget_*` — только для авторизованных, анонимный доступ закрыт полностью.
  - `booking_log` — анонимам только `insert` (раньше был открыт и на `select`, что позволяло читать имена/телеграм всех, кто когда-либо начинал запись).
- **budget.html** — обнаружено, что таблицы бюджета были полностью открыты анонимному ключу (страница вообще не используется, но была доступна без входа кому угодно). После RLS страница стала нерабочей для всех (у неё нет своего входа) — сознательно оставлено как есть, бюджетом не пользуемся.
- **Git-история** — проверена целиком (все коммиты) на предмет случайно закоммиченных секретов (токен бота, service_role ключ) — не найдено ничего, кроме публичного anon key (не секрет). Чистка истории не потребовалась.
- Проверено вручную: онлайн-запись (booking.html) целиком, вход и работа CRM под обоими логинами.
- **Осталось**: сделать репозиторий приватным (GitHub Pro), защита `ginter-crm` — отложено по просьбе Анны.

### 2026-07-22
- **booking.html / js/booking.js / css/booking.css** — переработаны описания видов приёма на шаге «Выберите вид приёма»:
  - Порядок карточек изменён: Первичный приём → Подбор и обучение контактным линзам → Контрольный визит → Помощь при оформлении заказа → Повторный приём → Экспресс-диагностика.
  - Первичный приём и Подбор КЛ — раскрывающийся блок «Что входит в приём» (список этапов приёма) + медицинская сноска о том, что оптометрист не проводит циклоплегию, осмотр глазного дна и измерение ВГД (это делает офтальмолог) + ссылка на пост в канале «О том, как проходит приём» (https://t.me/optometrist_anna_ns/12).
  - Помощь при оформлении заказа — описание уточнено («Если есть рецепт на очки, но нужна помощь в оптике, в том числе с детскими очками»), добавлен раскрывающийся блок «Чем могу помочь» (подбор оправы, рекомендации по посадке/качеству/тех. ограничениям по рецепту, помощь с выбором линз, ремонт очков, проверка соответствия изготовленных очков рецепту).
  - Экспресс-диагностика — убран бейдж «Акция» и подпись «Ограниченное число мест» (это постоянная услуга, не акция); добавлены два раскрывающихся блока: «Что входит» (авторефрактометрия, острота зрения без/в очках, рекомендация по замене очков + итог) и «Подходит ли мне экспресс-проверка?» (когда подходит экспресс / когда нужен полный приём).
  - Везде сокращение «КЛ» в пользовательских текстах развёрнуто в «контактные линзы» (название карточек, `apptName`, список причин обращения `REASONS`).
  - Аккордеоны — новые CSS-классы `type-acc`, `type-acc-h`, `type-acc-b`, `type-note`, `type-tg`, `type-group` в `booking.css`, с мобильной адаптацией в `@media(max-width:600px)`.
  - `booking.html` → `css/booking.css?v=2`, `js/booking.js?v=3`.
  - Уточнено правило про слово «рецепт»: Анна не может ВЫПИСЫВАТЬ рецепты, но упоминать рецепт, который у пациента уже есть от офтальмолога — можно (используется в описании «Помощь при оформлении заказа»).

### 2026-07-10 (3)
- **supabase/functions/telegram-bot/index.ts** — сообщение-подтверждение пациенту:
  - Длительность приёма («Продолжительность приёма — ...») теперь берётся из вида записи (`DURATION_MAP` по `appt.type`), а не захардкожена как «1 час». Помощь в оптике / Контрольный визит / Экспресс-диагностика → 15 минут; Первичный/Повторный → 1 час; КЛ с обучением → 90–120 минут.
  - Для вида «Помощь в оптике» убран абзац «За 30 минут до приёма прекратите активную зрительную нагрузку...» — этот совет не относится к короткому визиту за заказом.
  - ⚠️ **Требуется ручной редеплой Edge Function** (Анна, из терминала): `npx supabase functions deploy telegram-bot --project-ref ncfqiznpilikwmpqhapb --no-verify-jwt` — правки в GitHub на сам бот не влияют, пока функция не задеплоена.
- **js/booking.js** — из вида «Экспресс-диагностика» убрана приписка «(акция)» и в русском, и в сербском apptName — этот вид приёма постоянный, не акционный. Затрагивает текст в подтверждении пациенту, уведомлении Анне и в списке приёмов в CRM (для новых записей; старые записи с «(акция)» в БД не переименовываются автоматически).
- **js/exam.js**:
  - В шапке карты обследования добавлена галочка-бейдж «🎁 Экспресс-преглед», если приём — экспресс-диагностика (определяется по `appointments.type`).
  - Чекбоксы «Причина обращения» в Анамнезе больше не полагаются на нечёткое сравнение подстрок — теперь список чекбоксов дополняется точными формулировками, которые пациент выбрал в анкете онлайн-записи (`patients.visit_reason`), поэтому галочка гарантированно стоит на реальном выборе пациента, даже если он не входит в стандартный список `VISIT_REASONS` (актуально для причин из анкет «Экспресс», «Контроль», «Помощь» — там формулировки другие).
  - `printExam()` (кнопка 🖨️ в карте обследования) переведена на печать через отдельное окно (`_openPrintWindow`), как уже было у карточки пациента и email — раньше печатала прямо со страницы `crm-v3.html`, из-за чего браузер добавлял в футер печати URL страницы (`annanvslv.github.io/...`) — то, что выглядело как «ссылка на гитлаб».

### 2026-07-10 (2)
- **booking.html / js/booking.js** — возрастные ограничения теперь применяются только к «полноценным» видам приёма (`primary`, `repeat`, `cl`, `express`). Для видов «Контрольный визит» и «Помощь при оформлении заказа» (15-минутные слоты) добавлен флаг `noAgeLimit:true` — дисклеймеры по возрасту («до 10 лет», «10–17 лет») больше не показываются и запись доступна на любой возраст.
  - Дисклеймер для пациентов 10–17 лет (`ad17`, ru+sr) дополнен пунктом: присутствие родителя или законного представителя на приёме обязательно.
  - `js/booking.js` — версия та же (`?v=2` в `booking.html`, менять не требовалось, файл переиспользуется без изменения имени).
- **js/patients.js** — карточка пациента (профиль) дополнена блоком «📝 Из анкеты онлайн-записи»: причина обращения, жалобы, используемая коррекция (`correction_types`), диоптрии со слов, глазные/общие заболевания, зрительные нагрузки, примечание пациента, промокод. Раньше эти поля сохранялись в БД при онлайн-записи (`booking.js` → `patients` insert), но нигде не отображались в CRM — `correction_types` и `promo_code` были не видны вообще ни в карточке, ни в карте обследования. Блок показывается только если есть хотя бы одно заполненное поле (не мешает карточкам, заведённым вручную).
  - Причина обращения/жалобы/заболевания частично дублируются в карте обследования (вкладка «Анамнез», уже была реализована ранее), но там видны только при первом открытии карты по конкретному приёму — в профиле пациента их не было вообще.

### 2026-07-10
- **Лог онлайн-записи: уникальные пользователи** — `session_id` в `booking_log` генерируется заново при каждом открытии страницы, поэтому по нему считаются визиты (сессии), а не люди — один человек, зашедший несколько раз, давал несколько session_id.
  - **js/booking.js** — добавлен `visitor_id`: постоянный ID, сохраняется в `localStorage` (`blog_visitor_id`), переживает перезагрузки и новые визиты. Передаётся во все записи `booking_log` (и через обычный insert, и через `blogBeacon`/`pagehide`).
  - **js/bookinglog.js** — в разделе «Лог записи» добавлены две карточки статистики: «Уникальных пользователей (7 дней)» и «Уникальных пользователей (30 дней)» — считаются как `Set` уникальных `visitor_id` за соответствующий период.
  - **⚠️ Требуется миграция БД** (Анна выполняет вручную в Supabase SQL Editor): `ALTER TABLE booking_log ADD COLUMN visitor_id TEXT;` — без этого столбца новые записи логов будут падать в ошибку на вставке `visitor_id`.
  - Старые записи в `booking_log` (до миграции) не будут иметь `visitor_id` — они просто не учитываются в счётчике уникальных (не искажают цифру).
  - `booking.html` → `js/booking.js?v=2`, `crm-v3.html` → `js/bookinglog.js?v=2`.

### 2026-07-08 (2)
- **Лог онлайн-записи + алерты** — добавлено отслеживание всей воронки записи на `booking.html`, чтобы видеть пропущенные/неудачные записи (раньше это было незаметно).
  - Новая таблица Supabase `booking_log` (миграция — см. SQL, применяется вручную): `session_id`, `event` (`started`/`reached_form`/`submit_attempt`/`success`/`error`/`abandoned`), `patient_name`, `telegram`, `type_id`, `error_text`, `appointment_id`, `created_at`. RLS: anon insert+select.
  - **js/booking.js** — на каждой сессии записи логируются события: открытие страницы, переход к анкете, попытка отправки, успех (с `appointment_id`), ошибка (с точным текстом ошибки Supabase — включая случай «слот уже занят»). Если пользователь закрыл вкладку/ушёл с сайта, дойдя до анкеты, но не завершив запись — событие `abandoned` отправляется через `fetch(keepalive)` в `pagehide`.
  - **js/bookinglog.js** (новый) + пункт меню «Лог записи» в CRM (иконка колокольчика, между Расписанием и Настройками) — таблица последних 30 дней, сгруппированная по сессии, с итоговым статусом (Успешно / Ошибка / Не закончил(а) / Не завершено — если 30+ минут без результата / В процессе). Наверху — карточки счётчиков за 7 дней и красный алерт-баннер, если есть проблемные попытки, с текстом ошибки прямо в таблице.
  - **crm-v3.html** — подключён `bookinglog.js?v=1`, добавлен nav-пункт.
  - **js/i18n.js** — добавлены ключи `nav_bookinglog` (ru/sr) и запись в `navMap`.
  - **js/router.js** — добавлен маршрут `bookinglog`.

### 2026-07-08
- **БД (Supabase, миграция вручную через SQL Editor)** — исправлен критический баг: онлайн-запись (`booking.html`) и добавление пациента в CRM падали с ошибкой `duplicate key value violates unique constraint "patients_patient_code_key"`. Причина: на `patients.patient_code` стоит `UNIQUE`, но не было триггера, генерирующего код при вставке — все новые пациенты вставлялись с `patient_code = NULL`, и после первой такой записи следующие давали конфликт дублей NULL.
  - Backfilled существующие пациенты без `patient_code` (99 записей, коды `2601`–`2699`).
  - Создана функция `generate_patient_code()` + триггер `trg_patient_code` (`BEFORE INSERT ON patients`), генерирующие код в формате `ГГ` + порядковый номер.
  - Побочный баг при первой версии функции: `LPAD(seq::TEXT, 2, '0')` не только дополнял, но и **обрезал** номер до 2 символов — при seq=100 получалось `10` вместо `100`, что давало дубль с уже существующим `2610`. Исправлено на `LPAD(seq::TEXT, GREATEST(2, LENGTH(seq::TEXT)), '0')` — теперь номер не обрезается при переходе за 99 (следующий код: `26100`).
  - Проверено тестовой вставкой и на живом сайте (онлайн-запись + добавление пациента в CRM) — работает.

### 2026-07-02
- **js/calendar.js** — исправлен баг: на узких слотах (15-минутные «short» и любые события высотой <28px) клик не открывал попап с кнопкой «Удалить слот» — крестик не был виден вообще, потому что для узких слотов действия вынесены в попап (не inline-кнопку). Причина: `JSON.stringify()` для данных попапа давал двойные кавычки, которые ломали HTML-атрибут `onclick=\"...\"`, тоже в двойных кавычках — браузер обрывал атрибут на первой же кавычке JSON, и `cg2SlotPopup` даже не вызывался. Добавлено экранирование `\"` → `&quot;` перед вставкой в атрибут (в трёх местах: попап для записи пациента, попап для брони Эрвина, попап для свободного слота). Теперь клик по 15-минутному (и любому узкому) слоту открывает попап с кнопками, включая «Удалить слот» / «Удалить запись».
- **js/calendar.js** — сетка расписания сужена: `CAL_START_H` 8→9, `CAL_END_H` 20→19 (было 08:00–20:00, стало 09:00–19:00), убраны лишние ранние/поздние часовые строки, расписание компактнее.
- **crm-v3.html** — версия `calendar.js?v=11`.

### 2026-06-30
- **js/analytics.js** — страница Аналитика переделана с нуля. Общая инфа сверху (пациенты/заказы/прегледи всего). Фильтры отчёта: период «Месяц» (с переключением месяцев) или «Период» (даты от/до), кнопка «Сформировать отчёт». Таблица заказов: дата, № заказа (с пакета), пациент, тип, оправа (код+цена), линзы (назв.+цена), работа, итого, статус, галочка «Преглед» (есть ли привязанный рецепт `examination_id`). Фильтр Все/Только очки/Только линзы. Отдельно суммы по очкам и по контактным линзам (КЛ не входит в зарплату). Таблица прегледов: дата, пациент, тип, цена, бейдж Платный/Бесплатный — только завершённые приёмы. Блок «Расчёт зарплаты»: ручной ввод процента с заказов, чекбокс «учитывать только заказы дороже X дин.» (порог редактируемый), кнопка «Пересчитать» — сохраняет процент/порог в `settings` (ключи `salary_percent`, `salary_threshold_on`, `salary_threshold_val`) и пересчитывает: % от суммы очковых заказов + 100% от суммы прегледов. Кнопка «🖨️ Печать отчёта» формирует печатную версию (через переиспользуемый `_openPrintWindow` из exam.js) с таблицами заказов/прегледов и блоком зарплаты, открывается в новом окне с авто-вызовом печати.
- **crm-v3.html** — версия `analytics.js?v=12`.

### 2026-06-07
- **css/crm-mobile.css** — мобильная навигация: исправлен z-index конфликт (overlay `10001` > mobile-nav `10000`), overlay теперь `inset:0` (весь экран), `#modal-container` с `padding-bottom` = высота навбара + safe-area, модалки `max-height: calc(100dvh - mobnav - safe-area)`. Добавлена поддержка `env(safe-area-inset-bottom)` в navbare, `#app`, `#toast`.
- **js/auth.js** — убрана JS-логика `style.display` для `#mobile-nav` (управление только через CSS), скрытие ervin-кнопок в mobile-nav.
- **js/i18n.js** — полный аудит переводов: добавлены ключи `sort_*`, `order_type`, `frame_price_label`, `lens_price_label`, `work_price_label`, `prepayment_label`, `extra_payment`, `issued_label`, `order_updated`, `order_created`, `status_updated`, `select_patient`, `issue_btn`, `mark_ready_btn`, `exam_no_data`, `exam_far_short`, `exam_comp_short`, `exam_near_short`, `exam_cl_short`, `exam_control`, `exam_open_btn`, `exam_none` — во всех языках (ru + sr).
- **js/patients.js** — сортировка: по имени А→Я/Я→А, по дате добавления (новее/старше), по дате рождения. Все хардкод-строки заменены через `t()`: `'лет'→t('years')`, `'Параметры не заполнены'→t('exam_no_data')`, `'Даль/Компьютер/Близь/МКЛ'→t('exam_*_short')`, `'Контроль:'→t('exam_control')`, `'Otvori'→t('exam_open_btn')`, `'Spreman/Preuzmi'→t('mark_ready_btn')/t('issue_btn')`, `'Итого/Предопл/Ост'→t('total/prepayment/balance')`. Плашки пациентов через `statusLabel()`. Загрузка через `t('loading')`.
- **js/orders.js** — сортировка: по дате (новее/старше), по сумме (↑↓), по сроку. Все хардкод-строки заменены: заголовок карточки, метки полей, кнопки, тосты, конфирмы. Статусы в `<select>` через `statusLabel()`. Telegram-сообщения переведены на сербский.
- **crm-v3.html** — версии скриптов обновлены: `i18n.js?v=11`, `patients.js?v=12`, `orders.js?v=13`, `auth.js?v=13`, `crm.css?v=3`.

### 2026-05-25
- **booking.html** — поле «Фамилия и имя» разделено на два: «Фамилия (латиницей)» и «Имя (латиницей)». Валидация раздельная, в БД пишется `Фамилия Имя`.
- **js/exam.js** — карта обследования при печати переведена на сербский. Карта пациента тоже. Заключение оставлено на русском.

### 2026-05-20
- **calendar.js** — исправлен SyntaxError: незакрытый шаблонный литерал в `bookErvinAt`.
- **crm-v3.html** — `@media print` переопределяет мобильные стили таблиц: `display:table!important`.

---

## Прогресс

| # | Задача | Статус |
|---|--------|--------|
| 1 | Нейминг | ⚪️ частично |
| 2 | Кнопки действий в карте | ⚪️ |
| 3 | Причины обращения | ✅ |
| 4 | Номер приёма | ✅ |
| 5 | № заказа в карточке | ✅ |
| 11 | ID пациента | ✅ |
| 12 | Дата рождения в таблице | ✅ |
| 10 | Новый пациент (серб.) | ⚪️ |
| 7 | PD дефолт | ✅ |
| 8 | Рецепт — выпадающие | ⚪️ |
| 9 | Рецепт — полное название | ⚪️ |
| 6 | Текст адаптации | ✅ |
| 13 | Цветовые плашки | ✅ |
| 14 | Мобайл: навигация и попапы | ✅ |
| 15 | Аудит вёрстки | ⚪️ |
| — | Booking: разделить имя | ✅ |
| — | Печать карты: сербский | ✅ |
| — | Сортировка Пациенты + Заказы | ✅ |
| — | Полный i18n аудит (ru+sr) | ✅ |
| — | Аналитика: детальный отчёт + ручная % зарплата | ✅ |
| — | Аналитика: печать отчёта + № заказа в таблице | ✅ |
| — | Расписание: фикс удаления 15-мин слотов + сужение сетки | ✅ |
| — | Фикс бага `patient_code` (онлайн-запись + добавление пациента) | ✅ |
| — | Лог записи: уникальные пользователи (visitor_id) | ✅ |
| — | Booking: возраст — исключение для помощи/контроля + пункт о родителях | ✅ |
| — | Карточка пациента: показ данных анкеты онлайн-записи | ✅ |
| — | Telegram-бот: длительность приёма по типу записи | ✅ (нужен редеплой) |
| — | Экспресс-диагностика: убрано «(акция)» | ✅ |
| — | Карта обследования: галочка «Экспресс-преглед» + точное совпадение причин | ✅ |
| — | Печать карты обследования через отдельное окно (без URL в футере) | ✅ |
| — | Booking: описания видов приёма (аккордеоны, новый порядок, без бейджа «Акция») | ✅ |
| — | Фикс "Ошибка записи" на booking.html (RLS insert-only, RPC grant, гонка patient_code, опечатка) | ✅ |
| — | Интеграция сайта в booking-флоу (акцент кнопки, ссылки на priem.html, приглашение в боте) | ✅ (нужен редеплой telegram-bot) |
